# 权益原型合并

权益业务后台的静态 HTML 原型合集。

## 使用方式

直接打开根目录的 `index.html`，或通过本地静态文件服务器预览：

```bash
python -m http.server 8000
```

然后访问 `http://localhost:8000/`。

## 项目结构

- `index.html`：原型入口
- `css/`：公共样式
- `js/`：导航脚本
- 其他 HTML 文件：各业务模块原型页面
